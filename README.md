# LJY2X2CubeSolver
一款解决二阶魔方的代码

# LJY2X2CubeSolver

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![Flask](https://img.shields.io/badge/Flask-2.0+-green.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)
![Status](https://img.shields.io/badge/Status-Production_ready-brightgreen.svg)

**[中文](#特性) | [English](#features)**

一个轻量级的 2×2 魔方求解器，提供基于 Web 的 3D 交互界面与高效的双向 BFS 求解算法。

🌐 **[在线体验](https://pcpziyuxfiwl.sealoshzh.site/)**  
📦 **[GitHub 仓库](https://github.com/Lijunyan111121/LJY2X2CubeSolver)**

---

## 特性

- **🎮 3D 交互界面**：基于 CSS3 变换的实时 3D 魔方模型，支持拖拽旋转
- **⚡ 高效求解**：采用双向广度优先搜索（Bidirectional BFS），平均求解步数 < 15 步
- **📱 移动适配**：针对触摸事件优化，完整支持手机端填色操作
- **🎯 状态校验**：内置魔方合法性验证，防止无效输入
- **🎬 动画回放**：可视化展示求解步骤，支持自定义播放速度
- **🎉 庆祝动画**：Canvas 粒子特效增强用户体验

## 快速开始

### 在线使用

直接访问在线部署版本： **[https://pcpziyuxfiwl.sealoshzh.site/](https://pcpziyuxfiwl.sealoshzh.site/)**

### 本地部署

#### 环境要求

- Python 3.8+
- Flask 2.0+
- 现代浏览器（支持 CSS3 3D Transform）

#### 安装步骤

```bash
# 克隆仓库
git clone https://github.com/Lijunyan111121/LJY2X2CubeSolver.git
cd LJY2X2CubeSolver

# 创建虚拟环境（推荐）
python -m venv venv
source venv/bin/activate  # Linux/macOS
# 或 venv\Scripts\activate  # Windows

# 安装依赖
pip install flask

# 启动服务
python app.py
```

服务将在 `http://localhost:5001` 运行。

## 项目结构

```
LJY2X2CubeSolver/
├── app.py                 # Flask 后端主程序
├── cube_solver.py         # 双向 BFS 求解算法
├── face_move.py           # 魔方旋转操作实现
├── validator.py           # 状态合法性验证
├── static/
│   └── index.html        # 前端 3D 交互界面
└── README.md
```

## 使用说明

### 1. 填色模式
- 点击色板选择颜色（黄/白/蓝/橙/红/绿）
- 点击魔方贴纸进行填色
- **白橙蓝角块** 已固定在后下左位置作为参考

### 2. 求解流程
1. 填满所有 24 个贴纸后，「求解魔方」按钮自动启用
2. 点击按钮，系统会：
   - 验证魔方状态合法性
   - 计算最优解（通常 < 15 步）
   - 自动播放 3D 还原动画
3. 支持调整动画播放间隔（0-2 秒）

### 3. 高级功能
- **随机打乱**：生成 15 步随机打乱序列
- **重播动画**：重复观看上次求解过程
- **拖拽旋转**：鼠标/触摸拖拽可旋转魔方视角

## 算法说明

### 状态表示
采用 **Solver Format** 编码，顺序为：`UFL, UBL, UBR, UFR, DFL, DBL, DBR, DFR`（每个角块 3 个贴纸，共 24 字符）

### 求解算法
- **双向 BFS**：从初始状态和目标状态同时进行搜索，相遇时拼接路径
- **剪枝策略**：
  - 避免连续操作同一面（如 R 后不可接 R'）
  - 禁止重复访问相同状态
- **平均性能**：求解时间 < 100ms

### 旋转实现
`face_move.py` 中预定义了 R/U/F 三种基本转动及其逆操作，通过字符串切片实现状态变换。

## API 文档

### POST `/solve`

请求体：
```json
{
  "cube": "YRBYOBYOGYRGWRBWOBWOGWRG"
}
```

响应：
```json
{
  "steps": ["R", "U'", "F2", "R'", "U"]
}
```

错误响应：
```json
{
  "error": "填色状态不可解: 颜色 R 的数量不为4 (当前: 5)"
}
```

## 开发指南

### 前端技术栈
- 纯原生 JavaScript（无框架依赖）
- CSS3 3D Transform + Perspective
- Canvas 2D 粒子系统

### 核心类
- `Piece`：角块对象，包含位置 `pos` 和旋转四元数 `rot`
- `animateMove()`：基于 Promise 的动画队列系统

### 调试技巧
```bash
# 打印求解步骤
python cube_solver.py

# 验证状态合法性
python validator.py
```

## 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

本项目采用 **MIT 许可证** - 详见 [LICENSE](LICENSE) 文件

## 致谢

- 魔方配色方案参考标准 Rubik's Cube 配色
- 3D 变换算法基于四元数旋转

---

**💖 如果这个项目对你有帮助，请给个 Star 支持一下！**
